// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Browser from "./Browser.res.mjs";
import * as Core__Option from "@rescript/core/src/Core__Option.res.mjs";

var body = Browser.getBody();

function create_menu() {
  var menu_items = [
    {
      name: "Início",
      slug: "index"
    },
    {
      name: "Login",
      slug: "login"
    },
    {
      name: "Criar conta",
      slug: "signup"
    },
    {
      name: "Cursos",
      slug: "course"
    }
  ];
  var menu = document.createElement("nav");
  var list = document.createElement("ul");
  menu_items.forEach(function (item) {
        var list_item = document.createElement("li");
        var anchor = document.createElement("a");
        anchor.innerText = item.name;
        anchor.href = item.slug + ".html";
        list_item.appendChild(anchor);
        list.appendChild(list_item);
      });
  menu.appendChild(list);
  body.prepend(menu);
}

function create_main() {
  var main = document.createElement("main");
  body.appendChild(main);
}

function create_footer() {
  var footer = document.createElement("footer");
  var hr = document.createElement("hr");
  var status_dialog = document.createElement("p");
  status_dialog.id = "status";
  status_dialog.innerText = "Conectando...";
  footer.appendChild(hr);
  footer.appendChild(status_dialog);
  body.appendChild(footer);
}

function create_user_dialog() {
  var footer = Core__Option.getExn(document.getElementsByTagName("footer")[0], "footer not found");
  var user_dialog = document.createElement("p");
  user_dialog.id = "user_dialog";
  footer.before(user_dialog);
}

function main() {
  create_menu();
  create_main();
  create_footer();
  create_user_dialog();
}

export {
  create_menu ,
  create_main ,
  create_footer ,
  create_user_dialog ,
  main ,
}
/* body Not a pure module */
